# Умножение матриц с использованием OpenCL

Реализация умножения целочисленных матриц на C++ с использованием OpenCL. Проект использует POCL для кроссплатформенности и CI/CD.

## 📁 Структура проекта

matrix-multiply-opencl/
├── .github/workflows/ # CI/CD конфигурация
├── src/ # Исходный код
│ ├── main.cpp # Основное приложение
│ └── matrix_multiply.cl # OpenCL ядра
├── tests/ # Тесты
│ └── test_matrix_multiply.cpp
├── CMakeLists.txt # Конфигурация сборки
├── LICENSE # MIT лицензия
└── README.md # Этот файл

text

## 🛠️ Сборка и запуск

### Требования
- Компилятор C++ с поддержкой C++17
- CMake 3.15+
- POCL (Portable Computing Language) или другая реализация OpenCL

### Установка зависимостей (Ubuntu/Debian)
```bash
sudo apt-get update
sudo apt-get install build-essential cmake pocl-opencl-icd libpocl-dev
Сборка проекта
bash
mkdir build
cd build
cmake ..
make
Запуск приложения
bash
./matrix_multiply
Запуск тестов
bash
./matrix_multiply_test

## Тестирование
Проект использует POCL для запуска полноценных OpenCL тестов на CI.

## Лицензия
MIT License - смотрите файл LICENSE.

 ## CI/CD
Настроен GitHub Actions с:

Сборкой проекта

Запуском тестов с POCL

Проверкой стиля кода
